{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "scope-enum": [2, "always", ["core", "utils", "events", "personalize", "search-api-client"]],
    "scope-case": [2, "always", ["kebab-case"]]
  }
}
